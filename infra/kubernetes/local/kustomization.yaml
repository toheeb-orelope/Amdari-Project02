apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base
  - serviceaccounts.yaml
  - db-init-configmaps.yaml

patches:
  - path: local-configmap.patch.yaml
  - path: local-apps.patch.yaml
  - path: local-databases.patch.yaml
